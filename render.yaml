services:
  - type: web
    name: guess-price-bot
    runtime: python
    plan: starter
    buildCommand: pip install .
    startCommand: guess-price-bot
    healthCheckPath: /health
    autoDeployTrigger: commit
